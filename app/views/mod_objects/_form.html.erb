<%= form_for(@mod_object) do |f| %>
  <% if @mod_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mod_object.errors.count, "error") %> prohibited this mod_object from being saved:</h2>

      <ul>
      <% @mod_object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
